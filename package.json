{
  "name": "mwa",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i assets/tailwind-input.css -o assets/tailwind.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.4"
  }
}
